# Copy values into your host/container environment or an uncommitted .env file.
# Never commit real secrets.
TASKS_OPS_TOKEN=replace-with-operator-managed-secret

# MCP adapter target. Defaults to http://127.0.0.1:8787 when omitted.
TASKS_OPS_URL=http://127.0.0.1:8787
